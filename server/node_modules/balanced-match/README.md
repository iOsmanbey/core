If the `str` contains more `a` than `b` / there are unmatched pairs, the first match that was closed will be used. For example, <code>{{a}</code> will match <code>['{', 'a', '']</code> and <code>{a}}</code> will match <code>['', 'a', ...]</code>.